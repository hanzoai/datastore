<!--
    The ClickBench `create.sql` attaches the `hits` table on a cached disk
    rooted at `/dev/shm/datastore/`. The server validates that any
    filesystem cache path lies inside `filesystem_caches_path` /
    `custom_cached_disks_base_directory`, so point both at the same root.
-->
<datastore>
    <filesystem_caches_path replace="replace">/dev/shm/datastore/</filesystem_caches_path>
    <custom_cached_disks_base_directory replace="replace">/dev/shm/datastore/</custom_cached_disks_base_directory>
</datastore>
