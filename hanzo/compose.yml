services:
  datastore:
    image: ghcr.io/hanzoai/datastore:latest
    container_name: datastore
    ports:
      - "8123:8123"  # HTTP interface
      - "9000:9000"  # Native TCP
      - "9009:9009"  # Interserver
    volumes:
      - datastore-data:/var/lib/datastore
      - datastore-logs:/var/log/datastore-server
      - ./schema.sql:/docker-entrypoint-initdb.d/schema.sql:ro
      - ./config.xml:/etc/datastore-server/config.d/hanzo.xml:ro
    environment:
      DATASTORE_DB: hanzo
      DATASTORE_USER: hanzo
      DATASTORE_PASSWORD: ${DATASTORE_PASSWORD:-hanzo_dev}
      DATASTORE_DEFAULT_ACCESS_MANAGEMENT: 1
    ulimits:
      nofile:
        soft: 262144
        hard: 262144
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8123/ping"]
      interval: 10s
      timeout: 5s
      retries: 3
    restart: unless-stopped

  grafana:
    image: grafana/grafana:latest
    container_name: hanzo-grafana
    ports:
      - "3030:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:-admin}
      GF_INSTALL_PLUGINS: grafana-clickhouse-datasource
    depends_on:
      - datastore
    restart: unless-stopped

volumes:
  datastore-data:
  datastore-logs:
  grafana-data:

networks:
  default:
    name: datastore
