#!/usr/bin/env bash

CUR_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
# shellcheck source=../shell_config.sh
. "$CUR_DIR"/../shell_config.sh


$DATASTORE_CLIENT "
DROP TABLE IF EXISTS test;
CREATE TABLE test
(
    x UInt8 EPHEMERAL,
    s String MATERIALIZED format('Hello {} world', x)
) ORDER BY ();
"

$DATASTORE_LOCAL "SELECT 12 AS x FORMAT TSV" | $DATASTORE_CLIENT "INSERT INTO test (x) FORMAT TSV"
$DATASTORE_LOCAL "SELECT 34 AS x FORMAT TSV" | $DATASTORE_CLIENT "INSERT INTO test (*, x) FORMAT TSV"
$DATASTORE_LOCAL "SELECT 56 AS x FORMAT Native" | $DATASTORE_CLIENT "INSERT INTO test (x) FORMAT Native"
$DATASTORE_LOCAL "SELECT 78 AS x FORMAT Native" | $DATASTORE_CLIENT "INSERT INTO test (*, x) FORMAT Native"

$DATASTORE_CLIENT "
SELECT s FROM test ORDER BY ALL;
DROP TABLE test;
"


$DATASTORE_CLIENT "
DROP TABLE IF EXISTS test;
CREATE TABLE test
(
    x UInt8 EPHEMERAL,
    s String DEFAULT format('Hello {} world', x)
) ORDER BY ();
"

$DATASTORE_LOCAL "SELECT 11 AS x FORMAT TSV" | $DATASTORE_CLIENT "INSERT INTO test (x) FORMAT TSV"
$DATASTORE_LOCAL "SELECT 22 AS x FORMAT Native" | $DATASTORE_CLIENT "INSERT INTO test (x) FORMAT Native"

$DATASTORE_CLIENT "
SELECT s FROM test ORDER BY ALL;
DROP TABLE test;
"


$DATASTORE_CLIENT "
DROP TABLE IF EXISTS test;
CREATE TABLE test
(
    x UInt8,
    s String MATERIALIZED format('Hello {} world', x)
) ORDER BY ();
"

$DATASTORE_LOCAL "SELECT 10 AS x FORMAT TSV" | $DATASTORE_CLIENT "INSERT INTO test (x) FORMAT TSV"
$DATASTORE_LOCAL "SELECT 30 AS x FORMAT Native" | $DATASTORE_CLIENT "INSERT INTO test (x) FORMAT Native"

$DATASTORE_CLIENT "
SELECT s FROM test ORDER BY ALL;
DROP TABLE test;
"


$DATASTORE_CLIENT "
DROP TABLE IF EXISTS test;
CREATE TABLE test
(
    x UInt8,
    s String DEFAULT format('Hello {} world', x)
) ORDER BY ();
"

$DATASTORE_LOCAL "SELECT 88 AS x FORMAT TSV" | $DATASTORE_CLIENT "INSERT INTO test (x) FORMAT TSV"
$DATASTORE_LOCAL "SELECT 99 AS x FORMAT Native" | $DATASTORE_CLIENT "INSERT INTO test (x) FORMAT Native"

$DATASTORE_CLIENT "
SELECT s FROM test ORDER BY ALL;
DROP TABLE test;
"


$DATASTORE_CLIENT "
DROP TABLE IF EXISTS test;
CREATE TABLE test
(
    x UInt8 EPHEMERAL,
    s String MATERIALIZED format('Hello {} world', x)
) ORDER BY ();
"

$DATASTORE_LOCAL "SELECT 12 AS x FORMAT TSV" | $DATASTORE_CURL "${DATASTORE_URL}&query=INSERT+INTO+test+(x)+FORMAT+TSV" --data-binary @-
$DATASTORE_LOCAL "SELECT 34 AS x FORMAT TSV" | $DATASTORE_CURL "${DATASTORE_URL}&query=INSERT+INTO+test+(*,x)+FORMAT+TSV" --data-binary @-
$DATASTORE_LOCAL "SELECT 56 AS x FORMAT Native" | $DATASTORE_CURL "${DATASTORE_URL}&query=INSERT+INTO+test+(x)+FORMAT+Native" --data-binary @-
$DATASTORE_LOCAL "SELECT 78 AS x FORMAT Native" | $DATASTORE_CURL "${DATASTORE_URL}&query=INSERT+INTO+test+(*,x)+FORMAT+Native" --data-binary @-

$DATASTORE_CLIENT "
SELECT s FROM test ORDER BY ALL;
DROP TABLE test;
"


$DATASTORE_CLIENT "
DROP TABLE IF EXISTS test;
CREATE TABLE test
(
    x UInt8 EPHEMERAL,
    s String DEFAULT format('Hello {} world', x)
) ORDER BY ();
"

$DATASTORE_LOCAL "SELECT 11 AS x FORMAT TSV" | $DATASTORE_CURL "${DATASTORE_URL}&query=INSERT+INTO+test+(x)+FORMAT+TSV" --data-binary @-
$DATASTORE_LOCAL "SELECT 22 AS x FORMAT Native" | $DATASTORE_CURL "${DATASTORE_URL}&query=INSERT+INTO+test+(x)+FORMAT+Native" --data-binary @-

$DATASTORE_CLIENT "
SELECT s FROM test ORDER BY ALL;
DROP TABLE test;
"


$DATASTORE_CLIENT "
DROP TABLE IF EXISTS test;
CREATE TABLE test
(
    x UInt8,
    s String MATERIALIZED format('Hello {} world', x)
) ORDER BY ();
"

$DATASTORE_LOCAL "SELECT 10 AS x FORMAT TSV" | $DATASTORE_CURL "${DATASTORE_URL}&query=INSERT+INTO+test+(x)+FORMAT+TSV" --data-binary @-
$DATASTORE_LOCAL "SELECT 30 AS x FORMAT Native" | $DATASTORE_CURL "${DATASTORE_URL}&query=INSERT+INTO+test+(x)+FORMAT+Native" --data-binary @-

$DATASTORE_CLIENT "
SELECT s FROM test ORDER BY ALL;
DROP TABLE test;
"


$DATASTORE_CLIENT "
DROP TABLE IF EXISTS test;
CREATE TABLE test
(
    x UInt8,
    s String DEFAULT format('Hello {} world', x)
) ORDER BY ();
"

$DATASTORE_LOCAL "SELECT 88 AS x FORMAT TSV" | $DATASTORE_CURL "${DATASTORE_URL}&query=INSERT+INTO+test+(x)+FORMAT+TSV" --data-binary @-
$DATASTORE_LOCAL "SELECT 99 AS x FORMAT Native" | $DATASTORE_CURL "${DATASTORE_URL}&query=INSERT+INTO+test+(x)+FORMAT+Native" --data-binary @-

$DATASTORE_CLIENT "
SELECT s FROM test ORDER BY ALL;
DROP TABLE test;
"
