openSSL:
  server:
    certificateFile: '/etc/datastore-server/config.d/WithoutPassPhrase.crt'
    privateKeyFile: '/etc/datastore-server/config.d/WithoutPassPhrase.key'
    caConfig: '/etc/datastore-server/config.d/rootCA.pem'
    loadDefaultCAFile: true
    verificationMode: 'none'
    cacheSessions: true
    disableProtocols: 'sslv2,sslv3'
    preferServerCiphers: true
  client:
    certificateFile: '/etc/datastore-server/config.d/WithoutPassPhrase.crt'
    caConfig: '/etc/datastore-server/config.d/rootCA.pem'
    loadDefaultCAFile: true
    verificationMode: 'none'
    cacheSessions: true
    disableProtocols: 'sslv2,sslv3'
    preferServerCiphers: true
